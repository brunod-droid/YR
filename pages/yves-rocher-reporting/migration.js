import Link from 'next/link'; import {useEffect,useState} from 'react'; import {readReports,writeReports,STORAGE_KEY} from '../../lib/reportStore';
export default function Migration(){const [txt,setTxt]=useState('');const [msg,setMsg]=useState('');useEffect(()=>setTxt(JSON.stringify(readReports(),null,2)),[]);function imp(){try{const data=JSON.parse(txt);writeReports(data);setMsg('Imported into this YR hub browser storage.')}catch(e){setMsg('Invalid JSON')}}function download(){const blob=new Blob([JSON.stringify(readReports(),null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='yr_reports_export.json';a.click()}return <main className="shell"><div className="nav"><Link href="/yves-rocher-reporting">Back</Link></div><section className="hero"><h1>Migration / Import Export</h1><p>Data from the old Customer Hub cannot move automatically because browser localStorage is linked to the old domain. Use this page to paste/export JSON.</p><p><button className="btn" onClick={download}>Download current YR data</button> <button className="btn" onClick={imp}>Import pasted data</button></p><textarea value={txt} onChange={e=>setTxt(e.target.value)} /><p>{msg}</p></section><section className="card"><h2>To export from the old hub</h2><p>Open the old Customer Hub on the same browser, press F12, go to Console, paste this and press Enter:</p><pre>{`copy(localStorage.getItem('${STORAGE_KEY}') || '[]')`}</pre><p>Then paste the copied JSON above and click Import.</p></section></main>}
